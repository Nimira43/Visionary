import React from 'react'

interface IProjectFormProps {
  formType ? : 'add' | 'edit'
}

function ProjectForm() {
  return (
    <div>
      Project Form
    </div>
  )
}

export default ProjectForm
