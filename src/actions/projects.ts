'use server'

import supabase from '@/config/supabase-db-config'

export const addNewProject = async (payload: any) => {
  try {
    
  } catch (error: any) {
    return (
      success: false,
      error: error.message
    )
  }
}